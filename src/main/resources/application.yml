bot:
  url: www
  name: some_strange_longpollingbot
  token: ${BOT.TOKEN}

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB.URL}
    username: ${DB.USER}
    password: ${DB.PASSWORD}
